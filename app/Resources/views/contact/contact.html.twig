{% extends "base.html.twig" %}

{% block title %}Contact{% endblock %}

{% block body %}

	<div class="content container">
		<div class="row">
			<div class="col s12 m12 l4">
				<h3>Openingsuren</h3>
				<ul id="uren-list">
                    {% for openingsuur in openingsuren %}
                    <li class="black-text">
                        {{ openingsuur.dag }}
                            {% if openingsuur.van and openingsuur.van|date('H:i') > '00:00'|date('H:i') %}
                                open van {{ openingsuur.van|date('H:i') }} tot {{ openingsuur.tot|date('H:i') }}.
                            {% else %}
                                gesloten.
                            {% endif %}
                        {% endfor %}
                    <li class="black-text">Ook op feestdagen gesloten.</li>
                </ul>
			</div>
			<div class="col s12 m12 l4">
				<div id="map" class=""></div>
			</div>

			<div class="col s12 m12 l4">
				<div id="adres" class="center">
					<h3>Adres</h3>
					<p>Competentiecentrum Horeca, ICT en Retail</p>
					<p>Somerstraat 22</p>
					<p>2018 Antwerpen</p>
					<p>Tel: 03 202 17 05</p>
					<p>Tel: 03 202 17 03</p>
					<p>email: <a href="mailto:hetseizoen@vdab.be"> restaurant 't seizoen</a></p>
				</div>
			</div>	
			

			
		</div>
	</div>

{% endblock %}

{% block javascripts %}
	<script type="text/javascript" src="{{ asset('js/map.js') }}"></script>
	<script src="https://maps.googleapis.com/maps/api/js?callback=initMap" async defer></script>
	
{% endblock %}