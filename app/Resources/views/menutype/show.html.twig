{% extends 'base.html.twig' %}

{% block body %}
    <h1>MenuType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menuType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menuType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('menutype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('menutype_edit', { 'id': menuType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
